# Production API Configuration
# Update this URL when deploying to your production backend server
# Default points to Render deployment - change to your actual production backend URL
NEXT_PUBLIC_API_URL=https://primepos-5mf6.onrender.com/api/v1
NEXT_PUBLIC_USE_REAL_API=true
NEXT_PUBLIC_APP_NAME=PrimePOS

# Note: For local development, create a .env.local file instead of modifying this file
# .env.local will override these settings during development
